import { CreateLoginActivityDto } from './create-login-activity.dto';

describe('CreateLoginActivityDto', () => {
  it('should be defined', () => {
    expect(new CreateLoginActivityDto()).toBeDefined();
  });
});
