import { LoginActivitySchema } from './login-activity.schema';

describe('LoginActivitySchema', () => {
  it('should be defined', () => {
    expect(new LoginActivitySchema()).toBeDefined();
  });
});
